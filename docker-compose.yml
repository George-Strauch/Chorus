services:
  bot:
    build:
      context: .
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    env_file: .env
    volumes:
      - ${HOME}/.chorus-agents:/home/appuser/.chorus-agents
      - ${SCOPE_PATH:-${HOME}}:/mnt/host
    restart: unless-stopped
