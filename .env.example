# === Discord ===
DISCORD_TOKEN=
DISCORD_GUILD_ID=          # Optional: restrict to one server during development

# === LLM Providers ===
# Provide at least one. Chorus validates keys on startup and discovers available models.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# === Defaults ===
DEFAULT_MODEL=claude-sonnet-4-20250514
DEFAULT_PERMISSION_PROFILE=standard    # "open", "standard", or "locked" (or custom name)
IDLE_TIMEOUT_MINUTES=30                # Minutes before auto-saving and clearing context

# === Paths ===
CHORUS_HOME=~/.chorus-agents           # Where agent directories live
